import numpy as np

# def softmax(x):
    # """Compute softmax values for each sets of scores in x."""
    # e_x = np.exp(x - np.max(x))
    # return e_x / e_x.sum()

# scores = [0.5, 4.0]
# print(softmax(scores))

a=10

def multiply(input):
	return(input*a)
	
result=multiply(2)
print(result)